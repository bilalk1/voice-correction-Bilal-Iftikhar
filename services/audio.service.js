const uploadFiletoS3 = (email) => {

// Upload file form the /uploads folder to s3 
// Generate the auth s2s token form the auth service.
// Call the /audio/process api of the audio service with the email.


}

module.exports = { uploadFiletoS3 }